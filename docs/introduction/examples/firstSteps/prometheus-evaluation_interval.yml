global:
  scrape_interval: 15s
  evaluation_interval: 5s # evaluate rules / 5"

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
        labels:
          app: "prometheus"